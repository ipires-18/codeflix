import { Typography, Box } from '@mui/material';

export const EditCategory = (): JSX.Element => {
  return (
    <Box>
      <Typography variant="h3" component="h1">
        EditCategory
      </Typography>
    </Box>
  );
};
